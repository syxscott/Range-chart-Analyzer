# 2026-07-29

- 对当前工作树完成代码与古生物科研严谨性只读审阅，报告写入 `outputs/code-review-2026-07-29.md`。
- 实测全量 pytest：687 collected，671 passed，16 skipped，1 warning；`tests_core.py` 独立运行 375 passed；compileall 通过。
- 已复现 P0：`ics_resolve_age_bound` 将 `Bed 7/9` 当作 7/9 Ma，污染 DwC/PBDB 年代并可产生 `max_ma < min_ma`。
- 已复现 High：`quality._score_biozone_order` 混合可解析床号和不可解析文本时按 `None/int` 排序触发 TypeError，server 评分调用未隔离。
- 还确认未知 endpoint/occurrence 默认成 observed/in_situ、range idx 与逐行 confidence 被降级到 `_extras`、真实 gold 数据为 0 个。
- 四个多 Agent 审阅任务均因 HTTP 403 鉴权失败，报告已明确说明未取得任何子 Agent 结论。
- 完成所有可直接通过代码修复的已知高优先级缺陷：显式 Ma 单位与 DwC/PBDB 年龄方向保护、质量评分双层隔离、unknown/idx/逐行 confidence 的 Python/JS 契约、结构化 `_extras` 聚合，以及分类学 strict/lenient 双指标。
- range-chart Prompt 升级为 v4，并修复 `js/prompt.js` 的既有换行字符串语法错误；缓存版本回归测试改为不依赖固定未来版本。
- 修复后验证：Pytest 661 collected / 645 passed / 16 skipped / 0 failed；`tests_core.py` 375 passed；前端 117 passed；JS 聚合 45 passed；JS 对比 34 passed；compileall、核心导入、JS 语法与 diff 检查通过。
- 当前隔离环境仍缺少可选 PySide6 和 keyring：Fluent GUI 未实际导入，API Key 回退只提供混淆保护。真实专家 gold 数据不足仍是科研验证缺口。
