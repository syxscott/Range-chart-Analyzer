# Range Chart Analyzer — 完整修复计划（FIX-PLAN-2026-07-25）

**生成日期**：2026-07-25
**依据**：`docs/REVIEW-2026-07-25.md` 审阅报告（20 条 P0/P1/P2 finding）
**测试基线**：pytest **223 passed / 9 skipped**；node tests_frontend.js **80 passed**；node tests_aggregate.js **37 passed**（全绿）
**目标**：将项目从 4.8/10 提升到专业级科研软件水平

> 注：planner 输出的 §1–§5.1 章节因 transcript 丢失未能保留；下文以审阅报告 + 已恢复的 §5.2–§9 + 实施时补全的细节作为执行依据。

---

## 1. 修复目标（DoD 量化指标）

修复完成后必须满足：

| 指标 | 当前 | 目标 |
|---|---|---|
| P0/P1 真实缺陷 | 7 项存在 | **全部修复 + 回归测试覆盖** |
| pytest 全量 | 223/9sk | **≥ 270 passed**（新增 47 回归用例） |
| node tests_frontend.js | 80 passed | **≥ 85 passed** |
| node tests_aggregate.js | 37 passed | **37 passed 不变**（parity 不破坏） |
| D/F 等级可达 | 不可达（consistency=1.0） | **可达** |
| SSRF _NoRedirect | 已回归 | **生效**（测试断言 302 阻断） |
| 合并嵌合共识行 | 可产生 | **不可产生** |
| xlsx other_fossils 字符串 | 丢弃 | **保留** |
| apply_table_edits 错位 | 错位 | **无错位** |
| cache key 完整性 | 缺 extra_body/chart_lang | **完整** |
| 日文术语 | 中式直译 | **日本原生用法** |
| 物种归一化丢字段 | 4 字段全丢 | **13 字段全保留** |
| Provider API key | providers.json 明文 | **Fernet 加密** |

---

## 2. 修复项编号与依赖

| 编号 | 文件:行 | 修复内容 | 风险 | 阻塞谁 |
|---|---|---|---|---|
| **P0-1** | server.py:340-370 | _make_pinning_opener() 注册 _NoRedirect | 低 | — |
| **P0-2** | aggregate.py:574,632-674 | 统一 columnar 合并函数 | 中 | D-1 |
| **P0-3** | extractor.py:359-369 | 显式 4 字段入 row 字典 | 低 | E-4 |
| **P0-4** | server.py:999-1007,1025 | slot→cached_data 字典 | 中 | — |
| **P0-5** | js/minimax.js:75-143 | _array_root 三 normalizer 解包 | 低 | E-4 |
| **P0-6** | exporter.py:587-588 | xlsx 字符串 other_fossils 分支 | 低 | — |
| **P0-7** | exporter.py:383-384 | data_row_idx 在 continue 分支递增 | 低 | — |
| **P1-1** | aggregate.py:500-680 | 完整行投票 + biozone/range 硬约束 | 高 | D-2 |
| **P1-2** | aggregate.py:200-300 | ICZN 命名人归一化 | 中 | — |
| **P1-3** | quality.py:34-46,380-390 | consistency 真实计算 | 中 | — |
| **P1-4** | cache.py:80-120 | extra_body+chart_lang 入哈希 | 低 | — |
| **P1-5** | history.py:100-350 | 编辑回写 provenance 链 | 中 | — |
| **P1-6** | js/minimax.js:85 | SP_KNOWN 13 字段对齐 | 低 | — |
| **P1-7** | prompt.py:80-200 | 日文地质术语重写 | 低 | — |
| **P1-8** | extractor.py:269-296 | _classify age+name 仅当含 zone 关键词才 biozone | 低 | — |
| **P2-1** | app.py:200-350 | EXPECTED_HOSTS 填充 | 低 | — |
| **P2-2** | gui_fluent_providers.py | Fernet 加密 API key | 中 | — |
| **P2-3** | tests_aggregate.js | Golden e2e fixture | 低 | — |
| **P2-4** | history.py:100-200 | PROV-O 序列化 | 中 | — |
| **P2-5** | exporter.py:1-100 | invariant validator 入口 | 低 | — |

**依赖箭头**：P0-3 → P1-6（共享 SP_KNOWN）；P0-2 → P1-1（合并函数复用）

---

## 3. 修复阶段

### Phase A — SSRF & 关键安全回归（P0-1）
**目标**：恢复 _NoRedirect SSRF 防护
**修改**：`server.py` 的 `_make_pinning_opener()` 末尾 `build_opener(_PinnedHTTPSHandler(), _NoRedirect())`
**测试**：`tests/test_ssrf.py` 新增 `test_combined_opener_blocks_redirect`：构造 302 → 169.254.169.254 → 断言 HTTPError
**DoD**：3xx 阻断测试通过；原 `test_ssrf.py` 全绿

### Phase B — 数据完整性 P0（P0-2, P0-3, P0-4, P0-5）
**目标**：物种字段保留 + 合并不变量 + 缓存正确性 + JS 顶层数组解包
**修改**：
- `aggregate.py:632-674` 改用 `_merge_primary_list` 统一入口
- `extractor.py:359-369` row 字典补 endpoint_kind/reworked/range_top_bed/range_base_bed
- `server.py:999-1007,1025` ok_datas → slot_cache dict
- `js/minimax.js` 三个 normalizer 顶部加 `_array_root` 解包
**测试**：
- `tests/test_aggregate_columnar.py`：columnar 二次合并不覆盖
- `tests/test_extractor_species_fields.py`：13 字段全保留
- `tests/test_server_cache_noncontig.py`：slot 0,2 命中，slot 1 miss → 不漏跑
- `tests_frontend/test_minimax_array_root.js`：浏览器端 _array_root 解包
**DoD**：4 项 finding 测试全绿

### Phase C — 导出保真 P0（P0-6, P0-7）
**目标**：to_xlsx 保留字符串 + 编辑不错位
**修改**：
- `exporter.py:587-588` to_xlsx 循环加字符串分支
- `exporter.py:383-384` apply_table_edits continue 分支也 `data_row_idx += 1`
**测试**：
- `tests/test_exporter_xlsx_fix.py`：字符串 other_fossils 在 xlsx 中保留
- `tests/test_exporter_apply_edits_fix.py`：删除第 3 行后保存，后续行 _extras 不错位
**DoD**：2 项 finding 测试全绿

### Phase D — 合并算法严谨化（P1-1, P1-2）
**目标**：消除嵌合共识行；学名归一化安全
**修改**：
- `aggregate.py` 引入完整行投票（先聚类相似完整行，再取众数行）+ biozone/range 硬约束校验
- `aggregate.py` ID 归一化：去括号/标点/年份外置，ICZN 规则
**测试**：
- `tests/test_aggregate_row_voting.py`：3 跑全部键 (S,X) 但每字段都不同 → 不产出嵌合行
- `tests/test_aggregate_iczn_normalization.py`："Smith, 1950" == "(Smith, 1950)" == "Smith 1950"
**DoD**：2 项 finding 测试全绿，parity 测试 37/37 通过

### Phase E — 质量 + 缓存 + 历史 + JS parity + prompt（P1-3..P1-8）
**目标**：consistency 真实计算 + 缓存键完整 + 编辑 provenance + JS SP_KNOWN 13 字段 + 日文术语 + _classify 修正
**修改**：
- `quality.py:380-390` 真正计算跨 run agreement + FAD<LAD + biozone 耦合
- `cache.py:80-120` 缓存键哈希加入 extra_body + chart_lang
- `history.py:100-350` 编辑回写时附加 edit_timestamp / prev_result_hash 链
- `js/minimax.js:85` SP_KNOWN 扩到 13 字段
- `prompt.py:80-200` 日文术语表重写（組=Formation，段=Member 在日本学术中对应不同的 unit）
- `extractor.py:269-296` _classify 修正：age+name 必须同时含 zone/zonule/assemblage/oppel 才 biozone
**测试**：
- `tests/test_quality.py`（修改）：consistency < 1.0 触发；纯空结果 grade=F
- `tests/test_cache_key_completeness.py`：相同图但不同 chart_lang/extra_body → 缓存不命中
- `tests/test_history_provenance.py`：编辑回写后历史 JSON 含 edit 链
- `tests_frontend/test_minimax_sp_known.js`：JS 端 13 字段保留（不被丢入 _extras）
- `tests/test_prompt_japanese_terminology.py`：日文組/段翻译
- `tests/test_extraction.py`（修改）：age+name 单独 → sections，含 zone 关键词 → biozone
**DoD**：6 项 finding 测试全绿，JS↔Python SP_KNOWN 一致

### Phase F — 工程化（P2-1..P2-5）
**目标**：Origin 校验生效 + API key 加密 + Golden e2e + PROV-O + 不变式校验
**修改**：
- `app.py:200-350` modern-UI 部署模式 EXPECTED_HOSTS 填充
- `gui_fluent_providers.py` Fernet 加密 API key（首次设置时输入 machine-specific key）
- `tests_aggregate.js` 增加 golden e2e fixture
- `history.py:100-200` PROV-O entity/activity/agent 三元组序列化
- `exporter.py:1-100` 入口 invariant validator
**测试**：
- `tests/test_providers_encryption.py`：providers.json 不含明文 key
- `tests/test_provenance.py`：编辑历史含 PROV-O 结构
- `tests/test_exporter_invariants.py`：物种行缺 species/section/range_base/range_top → 报警
- `tests_aggregate.js` Golden 1 个 case
**DoD**：5 项 finding 测试全绿

---

## 4. 风险与缓解

| 风险 | 缓解 |
|---|---|
| **修改 _normalize_species_into 可能破坏现有测试** | 现有测试只断言 8 字段显式值；新增 4 字段不冲突；跑 baseline 后即时回归 |
| **合并算法重构跨端 parity 漂移** | 重写后立即跑 `tests_aggregate.js` 37 case 全绿 + Python aggregate_parity 37 case 全绿 |
| **Server.py opener 修改影响其他 LLM 调用** | _NoRedirect 仅阻断 3xx，不影响 200/4xx/5xx；正常 LLM API 永不返回 3xx |
| **Fernet 加密后旧 providers.json 读取失败** | 提供解密回退逻辑 + 一次性迁移脚本 |
| **PROMPT_VERSION 升级让旧缓存失效** | cache.py P1-4 修复同时升级 schema_version，旧缓存自动失效 |
| **日文术语变更影响现有 i18n** | i18n 键不变，仅值更新；现有测试不动 |

---

## 5. 测试策略

### 5.1 总体方法
**TDD**：每条 P0/P1/P2 finding 都遵循
1. 先写最小回归测试（红）
2. 改最小代码使测试转绿
3. 跑全量测试（绿）
4. 跨端 parity 检查（如果是双端逻辑）

### 5.2 新增测试文件清单

| 阶段 | 新测试文件 | 用例数 | 用途 |
|---|---|---|---|
| A | `tests/test_ssrf.py`（追加） | 1 | _NoRedirect 在 server opener 中生效 |
| B | `tests/test_aggregate_columnar.py` | 3 | columnar 合并不变量 |
| B | `tests/test_extractor_species_fields.py` | 4 | 13 字段保留 |
| B | `tests/test_server_cache_noncontig.py` | 3 | slot 字典缓存 |
| B | `tests_frontend/test_minimax_array_root.js` | 4 | JS _array_root 解包 |
| C | `tests/test_exporter_xlsx_fix.py` | 3 | xlsx 字符串 |
| C | `tests/test_exporter_apply_edits_fix.py` | 2 | data_row_idx |
| D | `tests/test_aggregate_row_voting.py` | 4 | 完整行投票 |
| D | `tests/test_aggregate_iczn_normalization.py` | 2 | ICZN |
| E | `tests/test_quality.py`（追加） | 2 | consistency 真实 |
| E | `tests/test_cache_key_completeness.py` | 2 | 缓存键 |
| E | `tests/test_history_provenance.py` | 3 | 编辑链 |
| E | `tests_frontend/test_minimax_sp_known.js` | 2 | JS 13 字段 |
| E | `tests/test_prompt_japanese_terminology.py` | 1 | 日文术语 |
| E | `tests/test_extraction.py`（追加） | 2 | _classify |
| F | `tests/test_providers_encryption.py` | 3 | Fernet |
| F | `tests/test_provenance.py` | 3 | PROV-O |
| F | `tests/test_exporter_invariants.py` | 2 | 不变式 |
| F | `tests_aggregate.js`（追加） | 1 | Golden e2e |

**合计新增测试用例：47（40 Python + 7 JS）**

### 5.3 Parity 矩阵

| Python | JS | 检查点 |
|---|---|---|
| `rca_core/extractor.py:247-256` SP_KNOWN | `js/minimax.js:85` SP_KNOWN | 字段数一致（13） |
| `rca_core/aggregate.py:_mode` | `js/aggregate.js:rcaAggMode` | 平局规则一致 |
| `rca_core/aggregate.py:_merge_field_across_runs` | `js/aggregate.js:mergeScalarField` | 投票逻辑 |
| `rca_core/json_utils.py` | `js/json-utils.js` | Level-4 评分 |
| `rca_core/prompt.py` | `js/prompt.js` | ABUNDANCE/COLUMNAR prompt |
| `rca_core/quality.py:score_range_chart` | `js/quality.js:scoreRangeChart` | 调用入口 |
| `rca_core/exporter.py` | `js/export.js` | 导出字段 |

---

## 6. 完成判据（DoD）

每阶段 DoD 见 §3。

**全局 DoD**：
- `pytest tests/ -x -q` → 270+ passed，0 failed
- `node tests_frontend.js` → 85+ passed
- `node tests_aggregate.js` → 37/37 passed（parity 不变）
- P0 全部 7 项 finding 标记 fixed
- P1 全部 8 项 finding 标记 fixed
- P2 全部 5 项 finding 标记 fixed
- 输出 `docs/REPORT-FIX-2026-07-25.md` 验证报告

---

## 7. 工作量估计

| 阶段 | 修复项 | 估计小时 | 备注 |
|---|---|---|---|
| A | 1 | 1-2 | opener 合并 |
| B | 4 | 4-6 | 字段补全 + 合并 + 缓存 + JS |
| C | 2 | 1-2 | 小定点修复 |
| D | 2 | 6-10 | 合并算法重构 |
| E | 6 | 4-8 | 散布小修复 |
| F | 5 | 6-10 | 加密 + PROV-O + 不变式 |
| **合计** | **20** | **22-38h** | 单人多 agent 协作 |

**实施方式**：本人逐步执行（结合 Read/Grep/Edit/Bash + 必要时子 agent 协助大规模测试）

---

## 8. 实施文件总览

```
rca_core/aggregate.py      ─ P0-2, P1-1, P1-2
rca_core/extractor.py      ─ P0-3, P1-8
rca_core/exporter.py       ─ P0-6, P0-7, P2-5
rca_core/cache.py          ─ P1-4
rca_core/quality.py        ─ P1-3
rca_core/history.py        ─ P1-5, P2-4
rca_core/prompt.py         ─ P1-7
rca_core/llm.py            ─ (ref only)
server.py                  ─ P0-1, P0-4
app.py                     ─ P2-1
gui_fluent_providers.py    ─ P2-2
js/minimax.js              ─ P0-5, P1-6
js/aggregate.js            ─ P1-1 (JS side)
js/export.js               ─ (parity)
```

---

## 9. 全部 20 项快速参考

```
P0-1  server.py:340-370        Combined opener includes _NoRedirect
P0-2  aggregate.py:574,632-674 Remove columnar second-merge overwrite
P0-3  extractor.py:359-369     Explicit 4 fields in row dict
P0-4  server.py:999-1007,1025  slot_cache dict instead of ok_datas list
P0-5  js/minimax.js:75-143     _array_root unwrap in 3 normalizers
P0-6  exporter.py:587-588      String branch in xlsx for other_fossils
P0-7  exporter.py:383-384      data_row_idx++ in continue branch
P1-1  aggregate.py:500-680     Full-row voting + constraint validator
P1-2  aggregate.py:200-300     ICZN author normalization
P1-3  quality.py:34-46,380-390 Real consistency computation
P1-4  cache.py:80-120          extra_body + chart_lang in key hash
P1-5  history.py:100-350       Provenance fields on edit rewrite
P1-6  js/minimax.js:85 + ext.  SP_KNOWN = 13 fields matching Python
P1-7  prompt.py:80-200         Japanese geological terms corrected
P1-8  extractor.py:269-296     _classify: age+name+zone-keyword=biozone
P2-1  app.py:200-350           EXPECTED_HOSTS populated
P2-2  gui_fluent_providers.py  Fernet encryption for API keys
P2-3  tests_aggregate.js       Golden e2e fixture
P2-4  history.py:100-200       PROV-O serialization
P2-5  exporter.py:1-100        Invariant validator at export entry
```